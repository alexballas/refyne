<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	package="{{.JavaPkgPath}}"
	android:versionCode="{{.Build}}"
	android:versionName="{{.Version}}">

	<application android:label="{{.Name}}" android:debuggable="{{.Debug}}"
				 {{if .AdaptiveIcon}}android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round"{{end}}>
	<activity android:name="org.golang.app.GoNativeActivity"
		android:label="{{.Name}}"
		android:configChanges="orientation|keyboardHidden|uiMode"
		android:exported="true"
{{- if .ShareMimeTypes}}
		android:launchMode="singleTask"
{{- end}}
		android:theme="@android:style/Theme">
		<meta-data android:name="android.app.lib_name" android:value="{{.LibName}}" />
		<intent-filter>
			<action android:name="android.intent.action.MAIN" />
			<category android:name="android.intent.category.LAUNCHER" />
		</intent-filter>
{{- if .ShareMimeTypes}}
		<intent-filter>
			<action android:name="android.intent.action.SEND" />
			<category android:name="android.intent.category.DEFAULT" />
{{- range .ShareMimeTypes}}
			<data android:mimeType="{{.}}" />
{{- end}}
		</intent-filter>
		<intent-filter>
			<action android:name="android.intent.action.VIEW" />
			<category android:name="android.intent.category.DEFAULT" />
			<data android:scheme="content" />
{{- range .ShareMimeTypes}}
			<data android:mimeType="{{.}}" />
{{- end}}
		</intent-filter>
{{- end}}
	</activity>
	<receiver android:name="org.golang.app.FyneNotificationReceiver"
		android:exported="false" />
	</application>

	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.INTERNET" />
</manifest>
